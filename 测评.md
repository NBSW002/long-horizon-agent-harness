# DeepAgent 测评机制梳理

本文基于以下两份材料整理：

- 论文：`paper/deepAgents.pdf`
- 对应实现：`/Users/xiao_xiong/Desktop/code/DeepAgent-main`

重点回答四个问题：测评如何执行、各指标如何计算、指标代表什么、论文结果与开源脚本的口径是否完全一致。

## 1. 总体结论

DeepAgent 的测评不是用一个统一公式覆盖所有数据集，而是按任务类型采用不同判定器：

| 任务类型 | 数据集 | 主要判定依据 |
| --- | --- | --- |
| 通用工具调用 | ToolBench、API-Bank、TMDB、Spotify、ToolHop | 最终任务是否完成，以及关键工具/API 路径是否被覆盖 |
| 交互式环境 | ALFWorld、WebShop | 环境返回的终止状态和奖励 |
| 深度研究/问答 | GAIA、HLE | 从模型输出中提取最终答案，再用字符串、规则和 LLM 裁判判断等价性 |

论文在表 1、表 2 中统一说明“所有任务报告 `Pass@1`”。这里的 `Pass@1` 应理解为：**每个测试样本只运行一条 Agent 轨迹，统计该轨迹是否通过，再对全部样本取平均**。它不是对同一道题生成多个解后计算的传统 `pass@k`。

不过，代码中每个基准还会额外保存 `Path`、`Score`、`api_accuracy`、`rouge_l`、`llm_equal` 等过程或答案质量指标。因此阅读结果时应先确认具体数据集采用了哪个 evaluator，不能把所有字段都直接视为“最终成功率”。

## 2. 测评执行流程

入口是 `DeepAgent-main/src/run_deep_agent.py`。

1. 根据 `--dataset_name` 加载测试集、工具集或交互环境。
2. Agent 对每条样本生成一条推理和工具调用轨迹，记录：
   - `output`：模型最终输出；
   - `executed_tool_calls`：实际工具调用；
   - `interactions`：工具调用及返回结果；
   - `action_count`：动作数；
   - `success`、`reward`：交互环境的状态。
3. 开启 `--eval` 后，按数据集分发到专用 evaluator：
   - ToolBench：`src/evaluate/evaluate_toolbench.py`
   - API-Bank：`src/evaluate/evaluate_api_bank.py`
   - TMDB / Spotify：`src/evaluate/evaluate_restbench.py`
   - ToolHop、GAIA、HLE：`src/evaluate/evaluate_base.py`
   - ALFWorld：`src/evaluate/evaluate_alfworld.py`
   - WebShop：`src/evaluate/evaluate_webshop.py`
4. 输出两类文件：
   - `*.metrics.json`：逐样本结果、预测答案和逐题指标；
   - `*.metrics.overall.json`：全数据集均值及按类别聚合的结果。

论文附录给出的统一实验约束包括：最大动作数为 50；主结果使用 QwQ-32B 作为主推理模型、Qwen2.5-32B-Instruct 作为辅助模型。图 5 则通过改变最大动作数，观察 WebShop 与 GAIA 的性能随交互预算变化的趋势。

## 3. 通用指标定义

设测试集共有 \(N\) 个样本，第 \(i\) 个样本的二值任务成功标记为 \(s_i\in\{0,1\}\)。

### 3.1 Pass@1 / Success

\[
\mathrm{Pass@1}=\mathrm{Success}=\frac{1}{N}\sum_{i=1}^{N}s_i
\]

含义：一次实际运行的 Agent 轨迹是否完成任务。它衡量端到端能力，通常是最重要的指标。

注意：不同 benchmark 对“完成”的定义不同。例如，ToolBench 依赖 LLM 裁判，WebShop 依赖环境最终奖励，ToolHop 依赖最终答案精确匹配。

### 3.2 Path

对第 \(i\) 个样本，若存在 \(m_i\) 个标注的必要中间目标，Agent 覆盖其中 \(c_i\) 个，则：

\[
\mathrm{Path}_i=\frac{c_i}{m_i},\qquad
\mathrm{Path}=\frac{1}{N}\sum_{i=1}^{N}\mathrm{Path}_i
\]

含义：Agent 是否走到了标注所需的关键工具、API 或子任务节点。它反映过程完整性，但**不等价于最终任务正确**：

- Path 高、Success 低：工具或中间步骤基本覆盖，但参数、执行结果整合或最终回答有问题；
- Path 低、Success 高：可能走了与标注不同但可行的路径，或者 evaluator 对替代路径不够宽容。

### 3.3 Score / Reward

\[
\mathrm{Score}=\frac{1}{N}\sum_{i=1}^{N}r_i,\qquad r_i\in[0,1]
\]

含义：环境给出的连续奖励，衡量“完成得有多符合用户约束”。它比二值 Success 更细粒度，适用于 WebShop 这类存在部分满足的环境。

### 3.4 精确匹配、包含率、F1 与等价性

用于 GAIA、HLE 等最终答案任务。

- `EM`：归一化后的预测答案与标准答案完全相同；
- `acc`：归一化后的标准答案是否为预测文本的子串；
- `F1`：预测和标准答案按 token 重叠计算的调和平均；
- `math_equal`：使用 `utils/math_equivalence.py` 做有限的数学字符串规范化后比较；
- `llm_equal`：调用辅助模型判断语义或数学等价，返回 `Correct` 记为 1，否则为 0。

其中：

\[
P=\frac{|Tokens(pred)\cap Tokens(gold)|}{|Tokens(pred)|},\quad
R=\frac{|Tokens(pred)\cap Tokens(gold)|}{|Tokens(gold)|},\quad
F1=\frac{2PR}{P+R}
\]

这些指标主要刻画最终答案文本的正确性或接近程度；它们不直接衡量工具调用过程是否合理。

## 4. 各 Benchmark 的实际计算方式

### 4.1 ToolBench

论文配置：使用 G3 多步骤、多工具测试子集，100 个测试样本；同时评估“给定真实工具”和“开放集合自主检索”两种场景。

代码：`src/evaluate/evaluate_toolbench.py`

#### Success 的计算

1. 从 Agent 原始输出中提取最终答案。
2. 用 LLM 裁判判断该答案对用户问题是否“已解决”（`Solved` / `Unsolved` / `Unsure`）。
3. 若答案未解决或不确定，再让 LLM 裁判判断原问题在给定工具条件下是否可解（`Solvable` / `Unsolvable` / `Unsure`）。
4. 判定规则：
   - 答案已解决：通过；
   - 答案未解决，但任务不可解：通过；
   - 答案未解决，且任务可解：失败；
   - 仍为不确定：随机投为通过或失败。
5. 对同一条已生成轨迹重复判定 4 次（`evaluate_times=4`），以多数票作为该题 Success。

因此，代码保存：

- `pass_ratio = 通过次数 / 4`；
- `average_success_rate = 多数票通过题数 / 总题数`。

含义：ToolBench 的 Success 是对最终任务完成度的 LLM 裁判结果。4 次投票不是生成 4 条新轨迹，而是反复裁判同一条轨迹，所以论文的 `Pass@1` 仍对应“单次 Agent 执行”。

#### Path 的计算

对每题：

\[
\mathrm{Path}_i=
\frac{
|\mathrm{调用过的工具名}\cap\mathrm{标注相关API名}|
}{
|\mathrm{标注相关API名}|
}
\]

然后对全部样本求平均。工具名会先进行标准化再比对。

注意事项：

- 代码实际计算的是**相关 API 覆盖率均值**，不是严格的“完整且有序的调用路径正确率”；
- 工具顺序、参数、工具返回值、额外调用均不影响 Path；
- 脚本会检测工具幻觉，但 `not_hallucinate` 只被记录，没有参与最终通过/失败决策；
- 不确定裁判会随机打破平局，因此复现实验时存在非确定性。

### 4.2 API-Bank

论文数据规模：314 个对话、73 个 API、753 次调用。代码支持：

- Level 1：给定候选 API；
- Level 3：需要工具搜索，且不把 `ToolSearcher` 计为目标 API 调用。

代码：`src/evaluate/evaluate_api_bank.py`

#### API Accuracy / 论文中的 Path 对应含义

对每一个标注 API 调用，在预测工具调用集合中寻找一个尚未被使用、且同时满足下列条件的调用：

1. API 名称完全一致；
2. 参数字典完全一致。

\[
\mathrm{API\ Accuracy}=
\frac{\mathrm{正确匹配的API调用数}}{\mathrm{标注API调用总数}}
\]

它反映 API 调用路径的原子正确性。论文表 1 的 API-Bank 列名为 `Path`，而当前开源 evaluator 输出字段名为 `api_accuracy`；从计算逻辑看，两者表达的是同一类“标注 API 调用覆盖/正确率”能力，但字段名并未完全对齐。

#### Success

对每题：

\[
\mathrm{Success}_i=
\begin{cases}
1,& \text{该题全部标注 API 调用均被正确匹配}\\
0,& \text{否则}
\end{cases}
\]

最终 Success 是逐题 `Success_i` 的平均值。

含义：只有所有标注调用都做到“同名 + 参数字典完全一致”，该任务才算完成。

#### Rouge-L

代码额外计算最终回复相对标注回复的 Rouge-L F 值：

\[
\mathrm{Rouge\text{-}L}=
\frac{(1+\beta^2)PR}{R+\beta^2P}
\]

其中 \(P,R\) 基于最长公共子序列。它衡量回复表述与参考回复的词序重合程度。

重要限制：当前 API-Bank evaluator 虽然解析了工具返回结果，但真正的 API 正确判定**只检查 API 名和参数**，不检查预测的工具返回内容，也不检查调用顺序。因此它更接近“调用集合匹配”，不是完整的有状态执行轨迹验证。

### 4.3 TMDB 与 Spotify（RestBench）

论文中：

- TMDB：100 个问题、54 个工具、平均 2.3 次 API 调用；
- Spotify：57 个问题、40 个工具、平均 2.6 次 API 调用。

代码：`src/evaluate/evaluate_restbench.py`

对每题，先从模型输出的 `<tool_call>...</tool_call>` 中解析实际使用的 endpoint，再将 endpoint 标准化为动态工具名。

#### Path

\[
\mathrm{Path}_i=
\frac{
\mathrm{已使用的标注endpoint数}
}{
\mathrm{标注endpoint总数}
}
\]

#### Success

\[
\mathrm{Success}_i=
\mathbb{1}[\mathrm{Path}_i=1]
\]

即：只要所有标注 endpoint 都至少出现过一次，就算成功。

额外输出：

\[
\mathrm{Avg\ API\ Calls}=
\frac{\mathrm{解析出的工具调用总数}}{N}
\]

含义和限制：

- Path 衡量 endpoint 覆盖完整度；
- Success 衡量是否覆盖全部标注 endpoint；
- 调用顺序、参数、API 返回结果、最终文本回答和额外调用都不参与判定；
- 因而它衡量的是“是否找对并使用了必要接口”，而不是严格验证接口调用是否真正完成用户任务。

### 4.4 ToolHop

论文数据规模：995 个问题、3,912 个可执行工具，每题要求 3 到 7 次串行调用。

代码：`src/evaluate/evaluate_base.py` 中的 `evaluate_predictions_toolhop`。

#### Correct

从最终输出提取 `\boxed{...}` 中的答案，转小写后与标注答案做完全匹配：

\[
\mathrm{Correct}=
\frac{1}{N}\sum_i \mathbb{1}[\mathrm{pred}_i=\mathrm{gold}_i]
\]

论文表 1 的 `Correct` 即该指标，语义上可看作 ToolHop 的最终 Pass@1。

#### Path

每题数据包含多个 `sub_task` 及其标准中间答案。脚本从工具响应文本中搜索每个中间答案；被任何一次工具响应包含，即认为该子任务已解决：

\[
\mathrm{Path}_i=
\frac{\mathrm{在工具返回中出现的标注子答案数}}{\mathrm{标注子任务答案数}}
\]

限制：

- 使用字符串包含匹配，不验证调用的工具名、参数或先后顺序；
- 子答案被转为集合，重复子任务答案只计一次；
- 子答案恰好出现在无关工具输出中也可能被计为覆盖。

### 4.5 ALFWorld

论文将其定位为文本具身任务，Agent 通过移动、拿取、放置、打开等动作完成家庭场景目标。

代码：

- 环境封装：`src/envs/alfworld.py`
- evaluator：`src/evaluate/evaluate_alfworld.py`

#### Success

环境执行动作后返回 `done`。当前工具管理器在 `done=True` 时将：

```python
seq["success"] = True
seq["reward"] = 1.0
```

因此：

\[
\mathrm{Success}=\frac{\mathrm{done为真的任务数}}{N}
\]

该实现依赖 ALFWorld 环境的约定：终止意味着任务成功。若未来替换为“失败也会终止”的环境，需要改为基于真实 reward 或环境 success 字段判断。

#### Path

测试数据为每题提供正则形式的子目标。脚本在所有工具返回文本中搜索各子目标：

\[
\mathrm{Path}_i=
\frac{\mathrm{匹配到的子目标数}}{\mathrm{子目标总数}}
\]

含义：衡量任务中间状态是否按预期推进，例如是否看见目标物、是否拿起物品、是否到达目标位置。

### 4.6 WebShop

论文将 WebShop 作为在线购物任务：Agent 需要搜索商品、浏览页面、选择规格，并点击购买。

代码：

- 环境封装：`src/envs/webshop.py`
- evaluator：`src/evaluate/evaluate_webshop.py`

#### Score

点击 `Buy Now` 后，环境页面返回 `reward`，范围为 0 到 1：

\[
\mathrm{Score}=\frac{1}{N}\sum_i reward_i
\]

它表示所购买商品对用户约束的满足程度。例如品类、属性、价格等条件只部分满足时会得到介于 0 和 1 之间的分数。

#### Success

\[
\mathrm{Success}_i=\mathbb{1}[reward_i=1.0]
\]

只有环境给出满分奖励才算任务完全成功。

注意：`evaluate_webshop.py` 的注释提到了 `fail_rate`，但实际没有计算或输出该字段。

### 4.7 GAIA 与 HLE

论文中：

- GAIA：包含文本、多模态、文件处理任务；表 2 分别报告 `Text`、`MM`、`File` 和 `All`；
- HLE：从 2,500 题中抽样 500 题；表 2 报告 `Text`、`MM` 和 `All`。

当前仓库没有专门的 `evaluate_gaia.py` 或 `evaluate_hle.py`，二者会走 `src/evaluate/evaluate_base.py` 的通用 `run_evaluation`。

#### 答案提取

通用 evaluator 优先提取：

```text
\boxed{最终答案}
```

若没有该格式，则尝试读取 `ANSWER:` 后的文本。故 GAIA/HLE 的提示词和模型输出必须稳定遵循最终答案格式，否则会出现空预测并被记为 0 分。

#### 计算字段

对每题答案，脚本输出：

- `em`：归一化后完全一致；
- `acc`：标准答案是否包含在预测中；
- `f1`：token 级重叠；
- `math_equal`：有限规则的数学表达式规范化后是否一致；
- `llm_equal`：辅助 LLM 对语义等价的判断。

随后按 `problem_type` 聚合：

- GAIA：`text` / `mm` / `file`；
- HLE：`text` / `mm`。

`All` 应为全部题目的均值，`Text`、`MM`、`File` 为对应子集均值。

#### 需要特别注意的口径差异

论文表 2 把 GAIA/HLE 的结果记为 Pass@1，但当前公开代码默认会输出 `em`、`acc`、`f1`、`math_equal`、`llm_equal`，并**没有显式生成名为 `pass@1` 的总指标**。因此：

1. 从开源脚本可以确定其最终答案评估所使用的候选信号；
2. 但仅凭该仓库，无法严格证明论文表 2 最终填入的是 `EM`、`math_equal` 还是 `llm_equal`；
3. 若目标是复现论文数值，应固定辅助裁判模型、答案提取模板、网络搜索时间点和数据版本，并确认作者最终采用的字段。

此外，`math_equal` 并非通用符号推理器，只做 LaTeX 分数、根号、单位、空白等有限规范化，最终仍主要是字符串比较；对 GAIA/HLE 的开放式自然语言答案，`llm_equal` 通常更接近语义正确性，但也会引入裁判模型的不确定性。

## 5. 论文中的对比与消融如何读

### 5.1 主结果表

- 表 1：ToolBench、API-Bank、TMDB、Spotify、ToolHop；
- 表 2：ALFWorld、WebShop、GAIA、HLE；
- 每个数据集同时给出最终任务结果与过程质量指标，例如 `Success + Path`、`Success + Score`、`Correct + Path`。

理解原则：

1. 优先比较 `Success` / `Correct` / `All`，它们最接近端到端完成率；
2. 再看 `Path` / `Score`，判断收益来自过程覆盖更完整，还是最终结果整合更准确；
3. ToolBench、ToolHop 还区分“提供真实工具”和“开放集合自主检索”，后者更能衡量工具检索与调用的联合能力。

### 5.2 Memory Folding 消融

论文表 3 在 ToolBench、ToolHop、WebShop、GAIA 上取算术平均：

\[
\mathrm{Avg}=
\frac{\mathrm{ToolBench}+\mathrm{ToolHop}+\mathrm{WebShop}+\mathrm{GAIA}}{4}
\]

结果如下：

| 方法 | ToolBench | ToolHop | WebShop | GAIA | Avg |
| --- | ---: | ---: | ---: | ---: | ---: |
| DeepAgent-32B-RL | 64.0 | 40.6 | 34.4 | 53.3 | 48.1 |
| 去除 Memory Folding | 63.0 | 36.6 | 32.4 | 44.7 | 44.2 |

去除记忆折叠后：

- 总均值下降 `3.9`；
- GAIA 下降 `8.6`，是四项中最明显的下降；
- ToolHop、WebShop 也下降。

这说明该机制的收益主要体现在长程、多工具、多状态转换任务中。评价的不是“压缩文本写得好不好”，而是压缩后的 Agent 是否能继续正确调用工具并完成最终任务。

### 5.3 自主工具检索分析

论文表 4 的 `Avg` 是 ToolBench、ToolHop、TMDB、Spotify 四项的算术平均。DeepAgent 的“自主工具检索”从 42.0 提升到 52.6。

该实验的测评目的不是单独衡量检索召回率，而是衡量：**Agent 能否在任务推进中发现需要的新工具，并把检索结果转化为端到端任务成功。**

### 5.4 动作预算扩展分析

图 5 改变 WebShop 与 GAIA 的最大动作上限。横轴是允许的最大工具搜索/工具调用次数，纵轴是任务分数。

含义：

- 分数随动作上限提高，说明复杂任务需要足够的交互预算；
- DeepAgent 相比 ReAct 在更高动作预算下仍保持或扩大优势，说明额外动作被更有效地用于探索、状态维护和纠错；
- 该图不能单独说明模型“更高效”，还应结合相同成功率下的平均动作数、成本和延迟进一步分析。

## 6. 论文与代码的一致性及风险点

| 项目 | 一致性 | 说明 |
| --- | --- | --- |
| ToolBench Success / Path | 基本一致 | 代码提供最终答案 LLM 裁判和相关 API 覆盖率，但裁判存在随机兜底和 4 次投票。 |
| API-Bank Success / Path | 语义接近 | 论文使用 `Path`，代码输出 `api_accuracy`；实际是 API 名和参数的精确匹配。 |
| TMDB / Spotify | 基本一致但偏宽松 | 代码只验证 endpoint 覆盖，不验证顺序、参数和返回结果。 |
| ToolHop Correct / Path | 基本一致但偏宽松 | 最终答案是精确匹配；路径是中间答案在工具输出中的字符串覆盖。 |
| ALFWorld Success / Path | 基本一致 | Success 依赖 `done`，Path 依赖工具响应对正则子目标的匹配。 |
| WebShop Success / Score | 一致 | 直接使用环境 reward；满分才计 Success。 |
| GAIA / HLE Pass@1 | 存在缺口 | 论文汇报 Pass@1 分组结果，公开代码只明确输出多种答案匹配指标，未明确最终表格字段。 |

因此，当前代码适合用于**趋势性比较、消融对比和工程回归测试**；若用于严格复现论文数值或对外报告，应额外固定：

- 测试集版本和抽样方式；
- 主模型、辅助裁判模型及其服务版本；
- 温度、随机种子、最大动作数；
- 搜索引擎/浏览器工具的时间点和缓存；
- ToolBench 的多次裁判投票策略；
- GAIA/HLE 最终采用的判定字段。

## 7. 复现实验的建议命令

在 `DeepAgent-main` 目录下，配置好 `config/base_config.yaml` 中的模型、工具服务与 API Key 后：

```bash
python src/run_deep_agent.py \
  --config_path ./config/base_config.yaml \
  --dataset_name toolbench \
  --enable_tool_search \
  --eval
```

例如运行 GAIA：

```bash
python src/run_deep_agent.py \
  --config_path ./config/base_config.yaml \
  --dataset_name gaia \
  --eval
```

建议先使用 `--subset_num` 跑小规模样本，检查 `outputs/<dataset>.*.deepagent/` 下的：

- 原始 `run.*.json`：工具调用、环境响应、最终答案是否完整；
- `*.metrics.json`：逐题失败原因和路径覆盖；
- `*.metrics.overall.json`：总体均值及分组指标。

对于 Aegis 后续的自主会话记忆评测，建议沿用“最终任务成功率 + 过程质量 + 资源成本”三层指标，而不只看压缩率：

1. 最终成功率：长程任务是否完成；
2. 过程质量：子目标完成率、错误恢复率、重复失败率；
3. 资源成本：输入 token、压缩次数、工具调用次数、总时延。

这样才能验证记忆折叠是否真正提升了长程任务稳定性，而不仅仅是缩短了上下文。
